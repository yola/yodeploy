# Generated by yodeploy
{{for log in logfiles}}
  {{log.path}} {
    {{log.frequency}}
  {{if log.size is not None}}
    size {{log.size}}
  {{endif}}
    rotate {{log.rotate}}
  {{if log.create is not None}}
    create {{log.create}}
  {{endif}}
  {{for opt in log.options}}
    {{opt}}
  {{endfor}}
  {{if log.sharedscripts is not None}}
    sharedscripts
  {{endif}}
  {{if log.postrotate is not None}}
    postrotate
  {{log.postrotate}}
    endscript
  {{endif}}
  {{if log.prerotate is not None}}
    prerotate
  {{log.prerotate}}
    endscript
  {{endif}}
  {{if log.firstaction is not None}}
    firstaction
  {{log.firstaction}}
    endscript
  {{endif}}
  {{if log.lastaction is not None}}
    lastaction
  {{log.lastaction}}
    endscript
  {{endif}}
}

{{endfor}}
